import { registerRootComponent } from 'expo';
import App from './App';

// registerRootComponent ensures the app works correctly in both
// Expo Go (development) and production standalone builds.
registerRootComponent(App);
