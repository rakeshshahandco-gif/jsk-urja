services:
  # Backend Web Service (serves both API and React frontend)
  - type: web
    name: jsk-urja-backend
    env: node
    rootDir: .
    buildCommand: cd backend && npm install
    startCommand: node backend/src/index.js
    envVars:
      - key: NODE_ENV
        value: production
